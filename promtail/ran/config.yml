server:
  http_listen_port: 9081
  grpc_listen_port: 0

positions:
  filename: /tmp/positions-ran.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # This will be replaced by dynamic generation, but needed for initial startup  
  - job_name: placeholder
    static_configs:
      - targets:
          - localhost
        labels:
          job: placeholder
          __path__: /dev/null
